# Netlify redirects file for React Router
# This ensures all routes are handled by React Router instead of Netlify
/*    /index.html   200

# API redirects (if you want to proxy to your backend)
# /api/*  https://your-backend-url.com/api/:splat  200

# Optional: Redirect HTTP to HTTPS
# http://your-domain.netlify.app/*  https://your-domain.netlify.app/:splat  301!
